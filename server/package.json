{
  "name": "server",
  "version": "1.0.0",
  "description": "HTTP + WebSocket server which implements ping-pong mechanism between client and server",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anuj Khandelwal",
  "license": "ISC",
  "dependencies": {
    "ws": "^7.1.0"
  }
}
